{
  "watch": ["src", "database"],
  "ext": "ts,json",
  "ignore": ["src/**/*.test.ts", "dist", "uploads", "logs"],
  "exec": "ts-node --transpile-only src/server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
